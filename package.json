{
  "name": "tarot-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Local proxy that holds the Volcano Engine Ark (DeepSeek) API key server-side for 每日塔罗's AI reading feature.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.104.0"
  },
  "engines": {
    "node": ">=18"
  }
}
